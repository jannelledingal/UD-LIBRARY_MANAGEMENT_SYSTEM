UDLibrarySystem Eclipse project
1) Import this project into Eclipse as an existing Java project.
2) Add MySQL Connector/J to the project's Build Path.
3) Run XAMPP (Apache + MySQL). Create the database using the provided SQL script.
4) Run udlib.ui.HomeFrame as the app entry point.
